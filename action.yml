name: 'Delay Timer'
description: 'Pauses workflow execution for a specified number of milliseconds. Useful for waiting between steps or simulating delays in CI/CD pipelines.'
author: 'Your Name or Org'
branding:
  icon: 'clock'    # Consider 'clock', 'hourglass', or another time-related icon
  color: 'yellow'  # Choose a color that fits your brand

inputs:
  milliseconds:
    description: 'Number of milliseconds to wait before continuing execution.'
    required: true

outputs:
  time:
    description: 'The ISO 8601 formatted timestamp when the wait completed.'

runs:
  using: 'node20'
  main: 'dist/index.js'
    description: Your output description here

runs:
  using: node20
  main: dist/index.js
